# audio_convert internal helper

This helper is used internally by the DataMate audio ASR pipeline operator for audio normalization. User-facing behavior is described by the operator README and metadata.
